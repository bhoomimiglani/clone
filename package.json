{
  "name": "everthreads",
  "version": "1.0.0",
  "description": "Everthreads — Story-driven clothing for creators",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "scripts": {
    "build": "cd client && npm install && npm run build && cd ../server && npm install",
    "start": "node server/index.js"
  }
}
